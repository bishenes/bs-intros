###### 您是否会用 Markdown？

如果不会的话，别担心 – 学习它超级简单。Markdown XL 包含 25 种定义，您只需极少时间即可掌握它。您很快就可以无需依赖鼠标，一直输入。

到那时，即可通过 `⌘9` 或工具栏的 `A|` 按钮，访问可用定义的完整列表。

找到了吗？很好。现在让其拖放至旁边以供参考。这些定义以三种不同方式工作：它们标记整个段落（如标题、注释块），或者标记某个字或短语（如粗体、标记），或者添加所谓的文本对象（如链接、脚注）。

我们快速浏览一下所有可用定义及其各自的用途。
